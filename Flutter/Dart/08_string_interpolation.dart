void main() {
  var name = 'EJ';
  var age = 10;
  var greeting = 'Hi Hello, I\'m $name and I\'m ${age + 10}';
  print(greeting);

  // $ 기호 뒤에 변수 사용하기 (변수가 이미 존재할 때)
  // 계산할 때는 ${} 안에 변수 넣기
  // ', " 모두 사용 가능
  // escape는 \
}
